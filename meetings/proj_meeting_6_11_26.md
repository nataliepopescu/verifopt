# meeting

## agenda

updates from slack

## notes

way to get the type of rval?
- possible fallback

AFL fuzzer
- inputs for ripgrep to get to line youre wondering about/want to change

run pipeline (without much analysis) to see things w dynamic dispatches

benchmarks
- crates the have them/are fast, may already have been incentivized to do these
  things manually

download gentoo tree
- grep through ebuilds
- do they depend on rust
- if in rust category (/dev/rust)
    - if so: lib (maybe bin)
    - if not: bin

or macports repo tree? bsd?
- source-based package manager for a distro
    - differentiate lib vs not, and if it requires rust to be compiled

mae math: in two weeks
- collections w nested A<B<C<D>>> and know more about D, thats where it comes
  into place
- recursion
