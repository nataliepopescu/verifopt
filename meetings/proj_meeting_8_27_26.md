# meeting

## agenda

- initial perf numbers
    - put in slack

- binary code sizes

## notes

How much time does ripgrep spend around dynamic dispatch

Analysis gives you WHERE dynamic dispatch

Flamegraph?

Low fidelity/granularity

Try different caps

In stats file record WHEN cap

Ordering if cases is important (prefetching), fallback



Check pipelines
- compile non-verifopt code in the verifopt way without actually running verifopt (add option for not running verifopt)


Disable hyper threading 

Throttling

The power governer 
- decides what voltage to set the cpu in
- Power saving?
- Want to never throttle
- is a file: says devices cpu

Turbo
