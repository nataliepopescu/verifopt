# Eval Plan

things to exercise:
- diff numbers/degrees of variants
    - limitations/thresholds?
- enabling larger downstream optimizations
    - inlining
    - branch prediction
- nested dyn dispatch (e.g. visitor pattern)
- variant pruning via flow analysis (CHA vs verifopt)

